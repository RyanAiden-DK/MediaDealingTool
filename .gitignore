整体逻辑：
一、格式转换
1. 尺寸不变压缩图片：
JPEG 是一种有损压缩格式，压缩删掉的内容细小，文件体积小，兼容性强，所以目标是将上传的图片转换成 JPEG 格式再压缩
* 关于handle_image_mode：JPG格式只有RGB三个通道 不支持A-透明度，而RGBA/P/PNG 有透明度通道，强行转换会导致透明部分被自动填黑（PNG可兼容RGB）
所以用纯白色底色填充底面，把带有透明度的图贴上来，新建底面要RGB适配JPG，利用paste的蒙版参数依照原图为模具，只让不透明的部分印在白底上

二、几何变换
1. 缩放：
